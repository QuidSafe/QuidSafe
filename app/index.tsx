// Root URL - renders the landing page directly.
// Signed-in users get redirected to /(tabs) by AuthRedirect in _layout.tsx.
// Signed-out users stay here and see the marketing page.
export { default } from './landing';
